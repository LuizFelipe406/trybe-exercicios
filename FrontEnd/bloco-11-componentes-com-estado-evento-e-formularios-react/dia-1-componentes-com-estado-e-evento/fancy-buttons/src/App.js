import './App.css';
import Button from "./button";

function App() {
  return (
    <div>
      <Button buttonText="Primeiro Botão" buttonMessage="Mensagem do primeiro botão" />
      <Button buttonText="Segundo Botão" buttonMessage="Mensagem do segundo botão" />
      <Button buttonText="Terceiro Botão" buttonMessage="Mensagem do terceiro botão" />
    </div>
  );
}

export default App;
